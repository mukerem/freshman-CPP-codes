#include<iostream>
using namespace std;
int x=4;//global variable
int main()
{
string volts[10]={a,b,c,d,,e,f,g,h,i,j,k}
int i;
for (i = 0; i <10; i++)
cin>>volts[i];
maximum = volts[0]; // set the maximum to element 0
for (i = 1; i <10; i++)
{// cycle through the rest of the array
if (volts[i] > maximum) // compare each element with the maximum
maximum = volts[i];
}
cout<<"the maximum number is "<<maximum;
cout<<volts[3*5%4];
}
